# ============================================================
# Meeting Record Transcript Pipeline — Requirements
# Python 3.11 | CUDA 12.1
# ============================================================
#
# INSTALL ORDER:
#
# Step 1 — PyTorch (CUDA 12.1):
#   pip install torch==2.6.0+cu121 torchaudio==2.6.0+cu121 \
#       --index-url https://download.pytorch.org/whl/cu121
#
# Step 2 — Everything else:
#   pip install DeepFilterNet==0.5.6 DeepFilterLib==0.5.6 --no-deps
#   pip install -r requirements.txt
#
# ── CONFLICT NOTES ──────────────────────────────────────────
# [C1] torch: env hiện tại 2.1.2 → cần upgrade lên 2.6.0 (sherpa-onnx CUDAExecutionProvider)
# [C2] numpy 2.4.4 vs DeepFilterNet metadata <2.0: soft conflict,
#       code tương thích thực tế → install với --no-deps để bỏ qua constraint
#
# KHÔNG CÒN conflict lightning/NeMo vì Parakeet giờ dùng sherpa-onnx.
# ============================================================

# ── PyTorch (cài qua Step 1 với special index) ────────────────────────────
# torch==2.6.0+cu121
# torchaudio==2.6.0+cu121

# ── Audio enhancement ─────────────────────────────────────────────────────
# [C2] install với: pip install DeepFilterNet==0.5.6 DeepFilterLib==0.5.6 --no-deps
DeepFilterNet==0.5.6
DeepFilterLib==0.5.6
clearvoice==0.1.2

# ── VAD ───────────────────────────────────────────────────────────────────
# Silero VAD loaded via torch.hub at runtime (snakers4/silero-vad)
# Nếu muốn pin offline: pip install silero-vad==5.1.2

# ── ASR: Vietnamese (Zipformer) + English (Parakeet) via sherpa-onnx ──────
sherpa-onnx==1.13.2

# ── ASR: Chinese — SenseVoice (FunASR) ────────────────────────────────────
funasr==1.3.7

# ── ASR: Fallback + LID — Whisper large-v3 (faster-whisper) ──────────────
faster-whisper>=1.0.0

# ── Audio I/O ─────────────────────────────────────────────────────────────
soundfile>=0.12.1
librosa>=0.10.1
ffmpeg==1.4

# ── HuggingFace ───────────────────────────────────────────────────────────
huggingface_hub>=0.24.0
datasets>=2.0.0

# ── Summarization ─────────────────────────────────────────────────────────
openai>=1.0.0

# ── Web UI ────────────────────────────────────────────────────────────────
streamlit>=1.35.0
python-dotenv>=1.0.0
